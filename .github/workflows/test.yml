name: test

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-linux:
    name: test-linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: recursive
      - uses: actions/setup-go@v7
        with:
          go-version: ">=1.22.0"
          cache: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - name: Install desktop dependencies
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev libssl-dev libxdo-dev patchelf
      - name: Build libwg
        run: cd libwg && ./build.sh
      - name: Install frontend dependencies
        run: npm --prefix apps/desktop ci
      - name: Stage desktop helper
        run: npm --prefix apps/desktop run build:helper
      - name: Test Rust workspace
        run: cargo test --workspace
      - name: Lint desktop and helper
        run: |
          cargo clippy -p feilian-desktop --all-targets --no-deps -- -D warnings
          cargo clippy -p feilian-helper --all-targets --no-deps -- -D warnings
      - name: Test frontend
        run: |
          npm --prefix apps/desktop exec playwright install --with-deps chromium
          npm --prefix apps/desktop run build
          npm --prefix apps/desktop run test:e2e

  test-macos:
    name: test-macos
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: recursive
      - uses: actions/setup-go@v7
        with:
          go-version: ">=1.22.0"
          cache: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - name: Build libwg
        run: cd libwg && ./build.sh
      - name: Install frontend dependencies
        run: npm --prefix apps/desktop ci
      - name: Stage desktop helper
        run: npm --prefix apps/desktop run build:helper
      - name: Test native crates
        run: cargo test -p feilian-ipc -p feilian-helper-client -p feilian-helper -p feilian-desktop
      - name: Build macOS desktop
        run: npm --prefix apps/desktop run tauri -- build --no-bundle

  build-windows:
    name: test-windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: recursive
      - uses: actions/setup-go@v7
        with:
          go-version: ">=1.22.0"
          cache: false
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - uses: msys2/setup-msys2@v2
        id: msys2
        with:
          msystem: MINGW64
          update: true
          install: make mingw-w64-x86_64-gcc
          path-type: inherit
      - name: Expose MinGW runtime
        shell: pwsh
        run: '"${{ steps.msys2.outputs.msys2-location }}\mingw64\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append'
      - name: Configure Rust GNU toolchain
        shell: pwsh
        run: |
          rustup toolchain install stable-gnu
          rustup default stable-x86_64-pc-windows-gnu
      - name: Build libwg
        shell: msys2 {0}
        run: cd libwg/wireguard-go && make libwg && mv libwg.* ..
      - name: Install frontend dependencies
        run: npm --prefix apps/desktop ci
      - name: Stage desktop helper and Wintun
        run: npm --prefix apps/desktop run build:helper
      - name: Test IPC crate
        env:
          RUST_BACKTRACE: 1
        run: cargo test -p feilian-ipc
      - name: Test helper crate
        env:
          RUST_BACKTRACE: 1
        run: cargo test -p feilian-helper
      - name: Test helper client crate
        env:
          RUST_BACKTRACE: 1
        run: cargo test -p feilian-helper-client
      - name: Compile desktop tests
        run: cargo test -p feilian-desktop --no-run
      - name: Build Windows desktop
        run: npm --prefix apps/desktop run tauri -- build --no-bundle
